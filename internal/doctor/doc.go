// Package doctor composes the checks docs/TROUBLESHOOTING.md walks a user
// through into one report. The command gathers the facts, Assess judges
// them, and Format renders the lines, so the judgement runs without a
// daemon, a launchd, or a desktop under it.
package doctor
